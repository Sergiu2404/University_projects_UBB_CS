function A = aikten(x0, f0, x) # x vector
  n = length(x0);
  P = zeros(n);
  P(:, 1) = f0';

  #d(1, :);
  #n = zeros(size(x));

  for i=1:length(x0)
    for j=1:length(x0)
      M =
    endfor
  endfor

  A = p(n, n);
